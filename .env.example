# === Backend ===
DATABASE_URL=postgresql+asyncpg://tdpay:tdpay@postgres:5432/tdpay
POSTGRES_USER=tdpay
POSTGRES_PASSWORD=tdpay
POSTGRES_DB=tdpay

REDIS_URL=redis://redis:6379/0

# Секреты (генерируются при первом setup):
# python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=change-me-min-32-chars-please-use-secrets-module
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=
JWT_SECRET=change-me-min-32-chars

JWT_ACCESS_TTL_MIN=15
JWT_REFRESH_TTL_DAYS=30

# === S3 (MinIO в dev) ===
S3_ENDPOINT=http://minio:9000
S3_PUBLIC_ENDPOINT=http://localhost:9000
S3_BUCKET=tdpay
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_REGION=ru-1

# === SMTP (MailHog в dev) ===
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@tdpay.local
SMTP_TLS=false

# === SMS Aero ===
SMSAERO_EMAIL=
SMSAERO_API_KEY=
SMSAERO_SIGN=TDPay

# === QR Manager ===
QRM_BASE_URL=https://app.devwapiserv.qrm.ooo
# Тестовый ключ из документации QRM:
QRM_TEST_API_KEY=7m9EkxnW.vJ9ROl7Bl7pjKxp1e1n9Gw9IyV6M46M1

# === Telegram ===
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=

# === Платформа ===
PLATFORM_DOMAIN=tdpay.local
PLATFORM_URL=http://localhost:3000
PLATFORM_COMMISSION_PCT=0.8

# === Feature flags (выключены в MVP) ===
ENABLE_RATE_LIMITS=false
ENABLE_CAPTCHA=false

# === Frontend (переменные с NEXT_PUBLIC_ доступны в браузере) ===
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_PLATFORM_DOMAIN=tdpay.local
# Хост S3 для next/image remotePatterns (production: s3.timeweb.cloud)
NEXT_PUBLIC_S3_HOST=
# Хост S3 для next/image remotePatterns (production: s3.timeweb.cloud)
NEXT_PUBLIC_S3_HOST=
